name: Update Progress README

on:
  push:
    paths:
      - 'annotations/**.json'
      - 'update_readme.py'
      - 'config.json'

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run progress calculation script
        run: python update_readme.py

      - name: Commit text changes to main
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md progress_history.json progress_chart.svg
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update progress README" && git push origin HEAD:main)
